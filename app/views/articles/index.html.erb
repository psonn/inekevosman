<div class="jumbotron center">

<div id="article">
  <% if @articles.any? %>
  <div class="page-header">
  <h1>Artikelen (<%= @articles.count %>)</h1>
</div>
  <% end %>
  <%= link_to("Nieuw artikel", [:new, :article], :class => 'btn btn-primary') %>
  <table class="table table-hover">
  <thead>
  <tr>
    <th>Titel</th>
    <th>Inhoud</th>
    <th></th>
  </tr>
  </thead>
  <% @articles.each do |article| %>
  <tbody>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.body %></td>
      <td>
        <div class="btn-group">
  <button class="btn btn-default btn-sm dropdown-toggle" type="button" data-toggle="dropdown">
    Opties <span class="caret"></span>
  </button>
  <ul class="dropdown-menu" role="menu">
    <li><%= link_to("Toon artikel", article) %></li>
    <li><%= link_to("Bewerk artikel", edit_article_path(article)) %></li>
    <li class="divider"></li>
    <li><%= link_to("Verwijder artikel", article, method: :delete, confirm: "Weet u het zeker?") %></li>
  </ul>
</div>
      </td>
    </tr>
  </tbody>
  <% end %>
  </table>
</div>