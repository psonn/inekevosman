<% provide(:title, 'Homes') %>

<div class="panel panel-default">
  <div class="panel-heading"><h1>Homepagina's (<%= @homes.count %>)</h1></div>
  <div class="panel-body">
    <% if user_signed_in? %>
    <%= link_to("Nieuw artikel", [:new, :home], :class => 'btn btn-primary') %>
    <% end %>
  </div>
<table class="table table-hover">
  <thead>
  <tr>
    <th>Titel</th>
    <th></th>
  </tr>
  </thead>
  <% @homes.each do |home| %>
  <tbody>
    <tr>
      <td><%= home.title %></td>
      <td>
        <div class="btn-group">
  <button class="btn btn-default btn-sm dropdown-toggle" type="button" data-toggle="dropdown">
    Opties <span class="caret"></span>
  </button>
  <ul class="dropdown-menu" role="menu">
    <li>
      <%= link_to home_path(home) do %>
        <span class="glyphicon glyphicon-eye-open"></span> Toon home
      <% end %>
      </li>    
    <li>
      <%= link_to edit_home_path(home) do %>
        <span class="glyphicon glyphicon-edit"></span> Bewerk home
      <% end %>
      </li>
  </ul>
</div>
      </td>
    </tr>
  </tbody>
  <% end %>
  </table>
  <div class="panel-footer">
  </div>
</div>